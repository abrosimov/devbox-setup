devbox_projects_dir: "$HOME/Work"

# Profile-specific extras (concatenated with base lists in task files).
# devbox_extra_brew: []
devbox_extra_brew_casks:
  - orbstack         # Work machines use OrbStack; personal uses Docker Desktop.
devbox_extra_mcp_http_servers:
  - name: atlassian
    url: "https://mcp.atlassian.com/v1/mcp"
